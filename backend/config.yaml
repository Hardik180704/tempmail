server:
  port: ":8080"
  mode: "debug" # debug, release

database:
  host: "localhost"
  port: 5432
  user: "tempmail"
  password: "password"
  dbname: "tempmail_pro"
  ssl_mode: "disable"

redis:
  addr: "localhost:6379"
  password: ""
  db: 0

smtp:
  domain: "temp-mail.dev"
  port: 2525
  max_size: 10485760 # 10MB
